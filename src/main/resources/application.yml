server:
  shutdown: graceful


eureka:
  instance:
    hostname: localhost

spring:
  lifecycle:
    timeout-per-shutdown-phase: 10s
  security:
    user:
      name: admin
      password: 1234
  profiles:
    active: dev

